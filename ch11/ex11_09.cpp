#include <iostream>
#include <map>
#include <vector>
#include <algorithm>
#include <string>

int main() {
  std::map<std::string, std::vector<int>> m1;
  std::map<std::string, std::vector<std::size_t>> m2;
  
  
  return 0;
}
